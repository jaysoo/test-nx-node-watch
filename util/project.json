{
  "name": "util",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "util/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["util/**/*.ts"]
      }
    }
  },
  "tags": []
}
